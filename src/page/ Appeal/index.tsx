import styles from "./index.module.scss";

const Appeal = () => {
  return (
    <section className={styles.appeal}>
      <p className={styles.appeal_p}>問題解決できるデザインを</p>
    </section>
  );
};

export default Appeal;
