import styles from "./index.module.scss";

const Appeal = () => {
  return (
    <section className={styles.appeal}>
      <p className={styles.appeal__p}>問題解決 できるデザインを</p>
    </section>
  );
};

export default Appeal;
