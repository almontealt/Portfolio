import styles from "./index.module.scss";

const Appeal = () => {
  return (
    <section className={styles.appeal}>
      <p className={styles.appeal__p}>問題解決 できるデザイン</p>
    </section>
  );
};

export default Appeal;
