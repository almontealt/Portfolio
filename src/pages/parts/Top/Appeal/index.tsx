import styles from "./index.module.scss";

const Appeal = () => {
  return (
    <section className={styles.appeal}>
      <p className={styles.appeal__p}>問題解決 できるデザインを作る</p>
    </section>
  );
};

export default Appeal;
